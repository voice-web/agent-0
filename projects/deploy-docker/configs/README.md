# configs/ (legacy location)

Per-environment JSON that used to live here (`127.0.0.1.json`, `oci-vm.json`) was removed. Use **`deployments/<id>/config.json`** inside each deployment bundle instead, then run `scripts/compile.py` / `up.sh`.
