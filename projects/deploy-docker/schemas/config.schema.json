{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://worldcliques.dev/deploy-docker/config.schema.json",
  "title": "Environment config overlay",
  "type": "object",
  "required": ["schema_version"],
  "properties": {
    "schema_version": { "type": "string" },
    "env_name": { "type": "string" },
    "keycloak_env_file": {
      "type": "string",
      "description": "Optional path to keycloak.env (~ expanded). Relative paths are under deploy-docker root (parent of deployments/). Overrides default ~/.secrets/worldcliques/<env_name>/keycloak.env before fallback scan."
    },
    "service_overrides": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": { "enabled": { "type": "boolean" } },
        "additionalProperties": true
      }
    },
    "routing": {
      "type": "object",
      "properties": {
        "html_hosts": { "type": "array", "items": { "type": "string" } }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
