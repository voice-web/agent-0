# Bundled assets

`earth-equirect.jpg` is baked into the image so the default site globe works without mounting `GLOBE_LANDING_ASSETS`. Optional compose mounts can still override or extend `/srv/www/assets`.
