# internal/basic-http

**Deferred** — add a minimal first-party HTTP service here when you need a controllable upstream for Caddy tests or health probes.

Planned artifacts when implemented:

- `Dockerfile`
- `version.txt` — start at **`0.0.1`** (internal semver; bump when **your** code changes)
- `CHANGELOG.md`

See **`../POC.md`** Phase 6.
